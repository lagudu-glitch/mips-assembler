#ifndef __PARSE__
#define __PARSE__

#include <stdio.h>
#include <stdlib.h>
#include <string.h>

#define MAX_WORDS 100       // Max number of words in a line
#define MAX_CHARS 1024      // Max number of characters in a line

// functions
void _parse (char* fname);

#endif
